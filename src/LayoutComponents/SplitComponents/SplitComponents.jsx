//for 2 given components that I need to split screen with :
//Create a splitScreen comp (imported)

export const LeftHandComponent = ({ name }) => (
  <h1>
    {name}
  </h1>
);

export const RightHandComponent = ({ name }) => (
  <h1>
    {name}
  </h1>
);
